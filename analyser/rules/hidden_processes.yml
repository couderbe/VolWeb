title: Potential hidden processes
condition:
    intersect:
        - module: PsScan
        - module: PsList
        - attributes:
            PID : PID
            ImageFileName : ImageFileName
        - not: True